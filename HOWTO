# List of various instrutions for setup/config

# Local development
> Develop on the cli (localhost)
>> cd ~/dev/vim4rabbit
>> claude

# Setup Jira
> Atlassian.com > Profile > Security > API Tokens > create token
>> # Install official Atlassian MCP servier with Claude CLI
claude mcp add --transport sse atlassian https://mcp.atlassian.com/v1/sse -s user
claude:  /mcp | atlassian | authenticate/connect

# Setup pytest framework
# allow direnv tool to un/load env vars for setting up python test env framework
# -- Only need to run this once;  it will error the first time and mention running 'direnv allow'
cd ~/dev/vim4rabbit && direnv allow
python -m pytest tests/ -v
# coverage
.venv/bin/python -m pytest tests/ --cov=vim4rabbit --cov-report=term-missing

